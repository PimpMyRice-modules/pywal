# yaml-language-server: $schema=../../.json_schemas/module.json

run:
  - action: file
    target: ~/.cache/wal/colors
  - action: file
    target: ~/.cache/wal/colors.json
  - action: file
    target: ~/.cache/wal/colors-wal.vim
  - action: file
    target: ~/.cache/wal/userContent.css
